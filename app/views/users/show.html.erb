<%= render "share/sidebar", book: @book, user: @user %>

<h2>Books</h2>
<table>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspin="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |user| %>
    <tr>
      <td><%= image_tag book.get_image %></td>
      <td><%= book.name %></td>
      <td><%= link_to "Show", book_path(book.id) %></td>
    </tr>
    <% end %>
  </tbody>
</table>